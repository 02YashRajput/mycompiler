{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug MyCompiler",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/mycompiler",
            "args": ["${workspaceFolder}/input.txt"],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "preLaunchTask": "Make Build",
            "externalConsole": true
        }
    ]
}
